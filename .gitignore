node_modules/
dist/
.vite-react-ssg-temp/
.env
.env.production
.env.local
.DS_Store
*.log
.vercel/
.vscode/
.idea/

# The news-wire data is a build artifact — regenerated by the prebuild step on
# every deploy, never committed.
src/data/feeds.json

# The previous Hugo site, kept for reference. It carries its own .git, so it is
# excluded rather than committed as a gitlink.
000 Archive/
